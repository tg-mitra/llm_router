# Pinned dependencies for reproducible builds (requirement 5 / 18.6).
# Install with: pip install -r requirements.txt
# Then: pip install -e .   (installs the llm_router package itself, editable)

# Runtime
sentence-transformers==6.0.1
scikit-learn==1.9.0
numpy==2.5.3
PyYAML==6.0.3
joblib==1.6.0

# Training (loads HF_TOKEN from .env, see .env.example)
python-dotenv==1.2.3

# Testing
pytest==9.1.1
pytest-cov==7.0.0
